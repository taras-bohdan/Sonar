language: node_js
node_js:
- '10'
install:
- cd $PROJECT_PART && npm install
script:
- yarn run lint && yarn run coverage
notifications:
  webhooks: https://coveralls.io/webhook?repo_token=$coveralls_repo_token
env:
  global:
    - COVERALLS_PARALLEL=true
matrix:
  include:
    - name: "Client Side"
      env: PROJECT_PART=client
    - name: "Server Side"
      env: PROJECT_PART=server CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
      - libpcap-dev
